services:
  - type: web
    name: rossewij-site
    runtime: node
    plan: free
    rootDir: rossewij-site/server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SESSION_SECRET
        generateValue: true
